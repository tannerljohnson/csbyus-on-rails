const footerComponentStyles = theme => ({
  footer: {
    backgroundColor: theme.palette.background.paper,
    padding: theme.spacing.unit * 4,
    fontFamily: theme.Typography.fontFamily[0],
  },
  footerText: {
    fontFamily: theme.Typography.fontFamily[0],
  }
});

export default footerComponentStyles;
